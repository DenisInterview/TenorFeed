# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'

target 'TubiTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'
  pod 'SDWebImage'
  pod 'SDWebImage/GIF'
  # Pods for TubiTest

  target 'TubiTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
